services:
  app:
    build: ./backend
    container_name: news-app
    restart: always
    ports:
      - "80:8000"
    volumes:
      - ./backend:/app
    env_file:
      - ./backend/.env
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000